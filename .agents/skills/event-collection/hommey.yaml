version: 1.0.1
display_name: 出差事项收集
category: business
intent: event_collection
agent_name: event_collection
user_facing: true
risk_level: low
catalog_order: 20
progress_key: trip_details_collecting
answer:
  section_kind: trip
  require_section: false
memory_hooks:
  - agent: event_collection
    effect: update_active_trip
tools: [active_trip_context]
input_schema: schemas/input.json
output_schema: schemas/output.json
execution:
  - skill: event-collection
    agent_name: event_collection
    priority: 1
    on_failure: abort
    max_retries: 1
    reason: 收集并更新当前出差事项
    expected_output: 结构化当前出差任务与缺失信息
    query: 收集本次出差安排（出发地={origin} 目的地={destination} 日期={start_date} 天数={duration} 目的={purpose}）
