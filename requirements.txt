# 环境变量加载
python-dotenv==1.0.1

# 多智能体框架
agentscope==1.0.16
# agentscope 的 evaluate 模块运行时依赖（未在其依赖中声明，缺了会导致 import 崩溃）
tqdm
# AgentScope 1.0.16 仍使用 MCP 1.x 的 streamablehttp_client API。
mcp>=1.13,<2.0

# LLM 调用
requests==2.32.3
httpx==0.28.1

# RAG 与向量库（PostgreSQL + pgvector；embedding 默认走 SiliconFlow 云端）
pypdf==6.4.1
# 聊天附件 DOCX 解析（import 名 docx）
python-docx>=1.1.0
# 知识库 XLSX 结构化解析
openpyxl>=3.1.0,<4.0
# 扫描 PDF 页渲染为图片后进入 OCR fallback（import 名 fitz）
PyMuPDF>=1.24.0,<2.0
# 图片附件：解码校验 + 降采样后送视觉模型（图片理解需 HOMMEY_VISION_ENABLED=true）
Pillow>=10.0.0

# 可选：仅当 HOMMEY_RAG_EMBEDDING_BACKEND=local 时安装
# sentence-transformers==5.2.3

# FastAPI WebUI
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
# multipart 表单解析（附件上传 UploadFile 运行时必需）
python-multipart>=0.0.9
# MCP 1.x（agentscope 1.0.16 的依赖）要求 pydantic>=2.11.0，故放宽下限
pydantic>=2.11.0,<3.0.0

# 网络搜索（DDGS）
ddgs==9.10.0

# 短期记忆 Redis 存储
#redis==6.4.0
redis>=5.0.0,<7.0.0

# 长期记忆 PostgreSQL 存储
psycopg[binary,pool]

# 鉴权系统（JWT + bcrypt + 邮箱校验）
PyJWT>=2.8
passlib[bcrypt]>=1.7.4
bcrypt>=4.0,<4.1          # 收紧上限以规避 passlib 1.7 与 bcrypt≥4.1 的兼容告警（见 questions.md Q-F）
email-validator>=2.0      # pydantic v2 EmailStr 所需

# Test runner
pytest==8.3.4
pytest-asyncio==0.24.0
jsonschema>=4.20.0,<5.0

# Skill metadata parsing
PyYAML==6.0.2
